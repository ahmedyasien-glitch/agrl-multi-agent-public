services:
  - type: web
    name: agrl-multi-agent-v2
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: streamlit run app/main.py --server.address=0.0.0.0 --server.port=$PORT
    envVars:
      - key: DATABASE_URL
        sync: false
